using System;
using System.Collections.Generic;
using System.Text;

namespace Prototype_Object_Sample
{
    class TestClass
    {

        // should create a subject with a main teacher and list of substitutes
        // should create a copy of subject and compare it with the actual one
        // should create a deep copy of subject and compare it with the actual one after change (carefull should controll the LIST)
        // Should create a deep 2 copy of subject and compare it with the actual one after change (carefull should controll the LIST)
    }
}
